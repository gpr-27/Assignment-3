apiVersion: v1
kind: Secret
metadata:
  name: notewise-secrets
type: Opaque
stringData:
  # Replace these locally before applying, or create the secret via kubectl command.
  GROQ_API_KEY: "REPLACE_ME"
  JWT_SECRET: "REPLACE_ME"
